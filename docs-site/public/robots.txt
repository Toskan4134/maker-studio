User-agent: *
Allow: /

# Canonical sitemap (latest build). Note: on a GitHub Pages project path this
# file is served at /maker-studio/robots.txt, so it isn't auto-discovered at the
# domain root — submit the sitemap in Google Search Console too.
Sitemap: https://toskan4134.github.io/maker-studio/sitemap-index.xml
